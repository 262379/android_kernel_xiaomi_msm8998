name: Build chiron kernel (KernelSU-Next manual hook)

# 本 workflow 假定放在你 fork 的
# LineageOS/android_kernel_xiaomi_msm8998 仓库里的 .github/workflows/ 下,
# 直接对 checkout 出来的这份内核源码打补丁 + 编译,不再单独克隆一次内核仓库。
# 在 Actions 页手动触发时,右上角分支下拉框选你要编译的分支(比如 lineage-22.2)。

on:
  workflow_dispatch:
    inputs:
      defconfig:
        description: "defconfig 文件名(不含路径)"
        required: false
        default: "chiron_defconfig"
      anykernel_repo:
        description: "AnyKernel3 模板仓库(会用 anykernel/anykernel.sh 覆盖其中的 anykernel.sh)"
        required: false
        default: "osm0sis/AnyKernel3"

env:
  ARCH: arm64
  # 64-bit / 32-bit(compat vDSO)交叉工具链前缀,配合 LLVM=1 使用
  CROSS_COMPILE: aarch64-linux-gnu-
  CROSS_COMPILE_ARM32: arm-linux-gnueabi-

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout kernel source (this fork)
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Free disk space
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /opt/hostedtoolcache/CodeQL
          sudo docker image prune --all --force
          df -h

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            bc bison flex libssl-dev libelf-dev \
            build-essential ca-certificates curl git \
            python3 python3-pip \
            zip unzip device-tree-compiler \
            gcc-aarch64-linux-gnu gcc-arm-linux-gnueabi \
            binutils-aarch64-linux-gnu binutils-arm-linux-gnueabi

      - name: Fetch Proton Clang
        run: |
          git clone --depth=1 https://github.com/kdrag0n/proton-clang.git clang
      
          echo "$PWD/clang/bin" >> "$GITHUB_PATH"
      
          clang --version

      - name: Add KernelSU-Next to kernel source tree
        run: |
          curl -LSs "https://raw.githubusercontent.com/KernelSU-Next/KernelSU-Next/next/kernel/setup.sh" | bash -s legacy

      - name: Apply manual hook patches
        run: |
          python3 scripts/ksu_manual_patch.py \
            . \
            "arch/arm64/configs/${{ github.event.inputs.defconfig }}"

      - name: Show resulting patch (for CI log review)
        run: |
          git diff --stat || true
          tail -n 6 "arch/arm64/configs/${{ github.event.inputs.defconfig }}"
          
      - name: Patch KernelSU-Next for Linux 4.4
        run: |
          python3 - <<'PY'
          from pathlib import Path

          files = {
              "drivers/kernelsu/runtime/ksud_integration.c": [
                  (
                      "copy_to_iter(KERNEL_SU_RC + ksu_rc_pos,",
                      "copy_to_iter((void *)(KERNEL_SU_RC + ksu_rc_pos),"
                  ),
              ],

              "drivers/kernelsu/compat/kernel_compat.c": [
                  (
                      "kvfree(p);",
                      "kvfree((void *)p);"
                  ),
              ],

              "drivers/kernelsu/selinux/rules.c": [
                  (
                      "#include <uapi/linux/sched/types.h>",
                      "#include <linux/sched.h>"
                  ),
              ],

              "drivers/kernelsu/feature/adb_root.c": [
                  (
                      "ALIGN_DOWN(stackp - sizeof(kLdPreload), 8)",
                      "((stackp - sizeof(kLdPreload)) & ~7UL)"
                  ),
              ],
          }

          for f, patches in files.items():
              p = Path(f)
              if not p.exists():
                  print("Skip", f)
                  continue

              text = p.read_text()

              for old, new in patches:
                  if old in text:
                      text = text.replace(old, new)
                      print(f"Patched: {f}")
                  else:
                      print(f"Pattern not found: {f}")

              p.write_text(text)

          PY

      - name: Verify patches
        run: |
          echo "==== ksud_integration ===="
          grep -n "copy_to_iter" drivers/kernelsu/runtime/ksud_integration.c || true

          echo "==== kernel_compat ===="
          grep -n "kvfree" drivers/kernelsu/compat/kernel_compat.c || true

          echo "==== selinux ===="
          grep -n "sched.h" drivers/kernelsu/selinux/rules.c || true

          echo "==== adb_root ===="
          grep -n "ALIGN_DOWN" drivers/kernelsu/feature/adb_root.c || true
          grep -n "stackp - sizeof(kLdPreload)" drivers/kernelsu/feature/adb_root.c || true   

      - name: Build kernel
        run: |
          make -j"$(nproc)" O=out ARCH=${ARCH} \
            LLVM=1 LLVM_IAS=1 \
            CROSS_COMPILE=${CROSS_COMPILE} \
            CROSS_COMPILE_ARM32=${CROSS_COMPILE_ARM32} \
            ${{ github.event.inputs.defconfig }}
          make -j"$(nproc)" O=out ARCH=${ARCH} \
            LLVM=1 LLVM_IAS=1 \
            CROSS_COMPILE=${CROSS_COMPILE} \
            CROSS_COMPILE_ARM32=${CROSS_COMPILE_ARM32} \
            2>&1 | tee build.log

      - name: Debug output
        run: |
          echo "===== boot ====="
          find out/arch/arm64/boot -maxdepth 2 -type f

          echo "===== search Image ====="
          find out -name "Image*" -print

          echo "===== search dtb ====="
          find out -name "*.dtb" -print

          echo "===== search Image.gz ====="
          find out -name "*.gz" -print

          echo "===== search Image.lz4 ====="
          find out -name "*.lz4" -print

      - name: Collect kernel image
        run: |
          mkdir -p dist

          if [ -f out/arch/arm64/boot/Image.gz-dtb ]; then
            echo "Using Image.gz-dtb"
            cp out/arch/arm64/boot/Image.gz-dtb dist/
          elif [ -f out/arch/arm64/boot/Image.gz ]; then
            echo "Using Image.gz"
            cp out/arch/arm64/boot/Image.gz dist/
          elif [ -f out/arch/arm64/boot/Image ]; then
            echo "Using Image"
            cp out/arch/arm64/boot/Image dist/
          else
            echo "Kernel image not found!"
            find out -name "Image*" -print
            exit 1
          fi

          echo "===== dist ====="
          ls -al dist

      - name: Upload raw kernel image artifact
        uses: actions/upload-artifact@v4
        with:
          name: chiron-kernel-image-${{ github.run_number }}
          path: dist/*
          if-no-files-found: error

      - name: Fetch AnyKernel3 template
        run: |
          git clone --depth 1 "https://github.com/${{ github.event.inputs.anykernel_repo }}.git" AnyKernel3
          cp anykernel/anykernel.sh AnyKernel3/anykernel.sh

      - name: Copy kernel image into AnyKernel3
        run: |
          if [ -f dist/Image.gz-dtb ]; then
            cp dist/Image.gz-dtb AnyKernel3/Image.gz-dtb
          elif [ -f dist/Image.gz ]; then
            cp dist/Image.gz AnyKernel3/Image.gz
          elif [ -f dist/Image ]; then
            cp dist/Image AnyKernel3/Image
          else
            echo "No kernel image found in dist!"
            ls -al dist
            exit 1
          fi

      - name: Pack AnyKernel3 zip
        working-directory: AnyKernel3
        run: |
          zip -r9 "../AnyKernel3-chiron-ksu-next-manual-${{ github.run_number }}.zip" \
            * -x ".git*" "README.md"

      - name: Upload AnyKernel3 flashable zip
        uses: actions/upload-artifact@v4
        with:
          name: AnyKernel3-chiron-ksu-next-manual-${{ github.run_number }}
          path: AnyKernel3-chiron-ksu-next-manual-${{ github.run_number }}.zip
          if-no-files-found: error

      - name: Upload build log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-log-${{ github.run_number }}
          path: build.log
